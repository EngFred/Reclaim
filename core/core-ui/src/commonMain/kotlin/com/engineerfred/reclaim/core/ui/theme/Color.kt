package com.engineerfred.reclaim.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
// RECLAIM uses a calm, grounded palette — no aggressive reds or hyper-saturated
// tones. Recovery is about clarity and safety, not urgency.

// Primary — deep teal (trust, calm, forward motion)
val ReclaimTeal900   = Color(0xFF003D40)
val ReclaimTeal700   = Color(0xFF00646A)
val ReclaimTeal500   = Color(0xFF00868E)   // primary brand colour
val ReclaimTeal300   = Color(0xFF4DB8C0)
val ReclaimTeal100   = Color(0xFFB2E8EB)

// Accent — warm amber (milestone celebration, hope)
val ReclaimAmber500  = Color(0xFFFFB300)
val ReclaimAmber200  = Color(0xFFFFE082)

// Semantic — check-in status colours
val ColorClean       = Color(0xFF388E3C)   // GREEN  — SUCCESS
val ColorStruggled   = Color(0xFFF57C00)   // AMBER  — STRUGGLED
val ColorRelapsed    = Color(0xFFD32F2F)   // RED    — RELAPSED
val ColorNoData      = Color(0xFF9E9E9E)   // GRAY   — no check-in

// Neutrals
val NeutralWhite     = Color(0xFFFFFFFF)
val Neutral50        = Color(0xFFF7F9FA)
val Neutral100       = Color(0xFFECF0F1)
val Neutral300       = Color(0xFFBDC3C7)
val Neutral600       = Color(0xFF7F8C8D)
val Neutral800       = Color(0xFF2C3E50)
val Neutral900       = Color(0xFF1A252F)

// Surface / Background
val SurfaceLight     = Color(0xFFFFFFFF)
val SurfaceDark      = Color(0xFF121212)
val BackgroundLight  = Color(0xFFF7F9FA)
val BackgroundDark   = Color(0xFF0E0E0E)

// SOS — urgent but not aggressive
val SosRed           = Color(0xFFB71C1C)
val SosRedLight      = Color(0xFFFFCDD2)